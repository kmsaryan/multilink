# Third-party packages (install via pip)
pandas>=2.2,<3
matplotlib>=3.8,<4

# Note:
# Modules like sqlite3, socket, struct, uuid, os, time, and select are part of
# Python's standard library and are not installed through requirements.txt.
